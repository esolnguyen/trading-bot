"""MCP server entry points for the trading bot.

Each subpackage exposes a FastMCP server bound to one concern (ml, binance,
analysis, rag). Servers are launched via ``scripts/run_mcp_<name>.py``.
"""
